/**
 * Creates a copy of the specified data, with sensitive information
 * removed.
 *
 * @param  {Object}  data
 *
 * @return {Object}
 */
function sanitize (data) {
  const sanitizedData = { ...data }

  sanitizedData.password = '*'
  return sanitizedData
}

module.exports = sanitize
