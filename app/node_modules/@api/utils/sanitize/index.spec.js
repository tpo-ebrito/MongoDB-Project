/* eslint-env mocha */

/**
 * Code under test.
 * @type {any}
 */
// const T = require('./index.js')

// describe('the "sanitize" module', () => {
//     it('must export a function')

//     describe('the exported function')
//         it('must return an object')
//         context('when the input contains a property with private information')
//             const privateFieldNames = [
//                 'password',
//                 'emailAddress',
//                 'ssn'
//             ]

//             privateFieldNames.forEach((name) => {
//                 it('must be masked in the returned object')
//             })
// })
